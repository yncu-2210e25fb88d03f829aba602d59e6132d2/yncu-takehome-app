/*

// Import then uncomment the following code

import { blob } from "@repo/pkg-azure-blob/blob.js";

const { client, container } = blob({
  connectionUrl: process.env.AZURE_BLOB_CONNECTION_URL,
  containerName: process.env.AZURE_BLOB_CONTAINER_NAME,
});

export { client, container };
*/
